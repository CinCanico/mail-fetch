# Mail Fetch

Mail Fetch is a Python script that fetches all emails associated with the given username and password from a POP3 or IMAP server and backup the emails to a local directory in Mbox format.

## Interactive Usage

Set up your `config.ini` file then in your terminal:

```bash
#Optional
source bin/activate
pip install -r requirements.txt
python3 main.py
```
